import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.ubamyresume',
  appName: 'ubamyresume',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
